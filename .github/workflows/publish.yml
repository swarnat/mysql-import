name: Build and publish

on:
  workflow_dispatch:
    branches:
      - master
    release:
      types: [ created ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: JS-DevTools/npm-publish@v1
        with:
          token: ${{ secrets.NPM_TOKEN }}
          access: public
          check-version: true